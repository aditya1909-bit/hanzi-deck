<?xml version="1.0" encoding="UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HanziDeck.App">
  <Application.Resources>
    <ResourceDictionary>
      <Color x:Key="Background">#050505</Color>
      <Color x:Key="Surface">#111111</Color>
      <Color x:Key="ElevatedSurface">#191919</Color>
      <Color x:Key="Orange">#FF8000</Color>
      <Color x:Key="PrimaryText">#F5F5F5</Color>
      <Color x:Key="SecondaryText">#A3A3A3</Color>

      <Style TargetType="ContentPage">
        <Setter Property="BackgroundColor" Value="{StaticResource Background}" />
      </Style>
      <Style TargetType="Label">
        <Setter Property="TextColor" Value="{StaticResource PrimaryText}" />
      </Style>
      <Style TargetType="Entry">
        <Setter Property="TextColor" Value="{StaticResource PrimaryText}" />
        <Setter Property="PlaceholderColor" Value="{StaticResource SecondaryText}" />
        <Setter Property="BackgroundColor" Value="{StaticResource ElevatedSurface}" />
      </Style>
      <Style TargetType="Editor">
        <Setter Property="TextColor" Value="{StaticResource PrimaryText}" />
        <Setter Property="PlaceholderColor" Value="{StaticResource SecondaryText}" />
        <Setter Property="BackgroundColor" Value="{StaticResource ElevatedSurface}" />
      </Style>
      <Style TargetType="Picker">
        <Setter Property="TextColor" Value="{StaticResource PrimaryText}" />
        <Setter Property="TitleColor" Value="{StaticResource SecondaryText}" />
        <Setter Property="BackgroundColor" Value="{StaticResource ElevatedSurface}" />
      </Style>
      <Style x:Key="PrimaryButton" TargetType="Button">
        <Setter Property="BackgroundColor" Value="{StaticResource Orange}" />
        <Setter Property="TextColor" Value="#050505" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="Padding" Value="18,12" />
      </Style>
      <Style x:Key="SecondaryButton" TargetType="Button">
        <Setter Property="BackgroundColor" Value="{StaticResource ElevatedSurface}" />
        <Setter Property="TextColor" Value="{StaticResource PrimaryText}" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="Padding" Value="14,10" />
      </Style>
    </ResourceDictionary>
  </Application.Resources>
</Application>
